{
  "name": "dd-game",
  "version": "0.1.0",
  "private": true,
  "description": "Static initial draft for DD.Game, with the smart-contract prototype archived for reference.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "web:install": "cd web && npm install",
    "web:dev": "cd web && npm run dev",
    "web:build": "cd web && npm run build",
    "check": "npm run typecheck && npm run web:build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
